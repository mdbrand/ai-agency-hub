# Backfill

One-time script to pull the full SaferProducts.gov Recall API history, paginated
by date, and seed the `recalls` table with real annual base rates.

Purpose: pick the final MVP category (spec Section 7) from actual frequency
counts across the 4 candidates, not three weeks of newsletter snippets.

Not yet implemented.
