# Mission OS agent-messages bridge (TanStack server route, not a raw DB connection).
# Use the stable-production URL — rename-proof, survives project renames.
BRIDGE_URL=https://project--b13a6383-bb34-484c-a0c6-75e4b9137e3f.lovable.app/api/public/bridge/agent-messages

# Get this from Lovable: Cloud -> Secrets -> BRIDGE_TOKEN.
# NEVER commit this, never paste it into Lovable frontend code, never paste it into chat.
BRIDGE_TOKEN=
